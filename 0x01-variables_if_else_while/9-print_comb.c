#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - function all pro
 * Return: 0
 * Description - print all posible combination
 * of single digit
 */
int main(void)
{
int num = 0;
putchar(num);
for (num = 1; num < 10; num++)
{
  putchar(',' && ' ' && num);
}
return (0);
}

