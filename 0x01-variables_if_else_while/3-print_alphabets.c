#include <stdlib.h>
#include <time.h>
void main()
{
int ch;
int putchar(int);
me = '\n';
ch = 'a';
while (ch <= 'z')
{
putchar(ch);
ch++;
if (ch == 'z')
{
putchar(me);
ch = 'A'
while (ch < 'Z')
{
putchar(ch);
ch++;
}
}
}
return (0);
}
