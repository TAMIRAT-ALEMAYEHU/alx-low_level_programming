#include <stdlib.h>
#include <time.h>
void main()
{
int ch;
int putchar(int me);
me = '.\n';
ch = 'a';
while (ch <= 'z')
{
putchar(ch);
if (ch == 'e' || ch == 'q')
{
putchar(me);
ch++;
}
else
{
ch++;
}
}
return (0);
}
