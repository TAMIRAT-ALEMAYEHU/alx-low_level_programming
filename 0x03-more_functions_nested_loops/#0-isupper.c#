#include "main.h"
/**
 * _isupper - functions check the letter
 *
 * Return: 1 if the letter is upper otherwise 0
 */
int _isupper(int c
	     {

	       if (c >= 'A' && c <= 'Z')
		 return (1);
	       else
		 return (0);
	     }